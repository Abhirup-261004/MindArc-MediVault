<% pageCSS = ["map"]; %>
<link
  rel="stylesheet"
  href="https://unpkg.com/leaflet@1.9.4/dist/leaflet.css"
/>

 <header class="header">
    <div class="header-left">
        <div class="logo">🌱</div>
        <h1>MediVault Mapp</h1>
    </div>
</header>

<div class="main-container">
    <aside class="sidebar">

        <h2 class="section-title">Find Your Route</h2>

        <div class="input-group">
            <div class="input-wrapper">
                <span class="input-icon">📍</span>
                <input type="text" id="from-input" class="input-field" placeholder="From where?">
            </div>
        </div>

        <div class="input-group">
            <div class="input-wrapper">
                <span class="input-icon">🎯</span>
                <input type="text" id="to-input" class="input-field" placeholder="To where?">
            </div>
        </div>

        <button class="plan-button" onclick="planRoute()">Find Route</button>

        <div class="loading" id="loading">
            <div class="spinner"></div>
            <p style="margin-top: 0.5rem;">Calculating routes...</p>
        </div>

        <div class="mode-section">
            <h3 class="section-title">Mode</h3>
            <select class="mode-select" id="mode-select" onchange="updateRoute()">
                <option value="driving">🚗 Driving</option>
                <option value="walking">🚶 Walking</option>
                <option value="cycling">🚴 Cycling</option>
                <option value="transit">🚌 Public Transit</option>
            </select>
        </div>

        <div class="routes-container" id="routes-container"></div>

                <!-- Emergency Hospital Finder -->
        <div class="emergency-section">
            <h3 class="section-title">Emergency Help</h3>

            <p class="emergency-text">
                In case of emergency, find the closest hospital or medical center near you.
            </p>

            <button class="emergency-button" onclick="findNearbyHospitals()">
                🚑 Find Nearby Hospital
            </button>

            <small class="emergency-hint">
                Uses your current location (you’ll be asked for permission).
            </small>

            <div id="emergency-results"></div>
        </div>

        

    </aside>

    <div class="map-container">
        <div id="map"></div>
    </div>
</div>

<script src="https://unpkg.com/leaflet@1.9.4/dist/leaflet.js"></script>
<script src="/js/mapScript.js"></script>